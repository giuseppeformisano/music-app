name: Build APK

on:
  push:
    branches: [master, main]
    paths:
      - "app/**"
      - "*.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - ".github/workflows/build-apk.yml"
  workflow_dispatch:
    inputs:
      build_type:
        description: "Tipo di build"
        type: choice
        options: [debug, release]
        default: debug

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Ripristina debug.keystore
        env:
          DEBUG_KEYSTORE_BASE64: ${{ secrets.DEBUG_KEYSTORE_BASE64 }}
        run: |
          if [ -f debug.keystore ]; then
            echo "Uso il debug.keystore versionato nel repo."
          elif [ -n "$DEBUG_KEYSTORE_BASE64" ]; then
            echo "$DEBUG_KEYSTORE_BASE64" | base64 -d > debug.keystore
            echo "Keystore ripristinato dal secret."
          else
            echo "::warning::DEBUG_KEYSTORE_BASE64 non impostato. Genero un keystore temporaneo."
            keytool -genkeypair -v \
              -keystore debug.keystore \
              -storepass android -keypass android \
              -alias androiddebugkey \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=Android Debug,O=Android,C=US"
          fi

      - name: Mostra impronte di firma
        run: |
          keytool -list -v -keystore debug.keystore -storepass android -alias androiddebugkey \
            | grep -E "SHA1:|SHA256:"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.3.1"

      - name: Build APK
        env:
          BUILD_TYPE: ${{ inputs.build_type || 'debug' }}
        run: |
          if [ "$BUILD_TYPE" = "release" ]; then
            gradle :app:assembleRelease
          else
            gradle :app:assembleDebug
          fi

      - name: Crea version.json
        run: |
          VERSION_CODE=$(grep 'versionCode = ' app/build.gradle.kts | grep -oE '[0-9]+')
          VERSION_NAME=$(grep 'versionName = ' app/build.gradle.kts | grep -oE '"[^"]+"' | tr -d '"')
          echo "{\"versionCode\": $VERSION_CODE, \"versionName\": \"$VERSION_NAME\"}" > version.json

      - name: Pubblica APK
        env:
          GH_TOKEN: ${{ github.token }}
          BUILD_TYPE: ${{ inputs.build_type || 'debug' }}
        run: |
          TAG="latest-${BUILD_TYPE}"
          gh release delete "$TAG" --yes 2>/dev/null || true
          gh release create "$TAG" \
            --prerelease \
            --title "Latest ${BUILD_TYPE} build" \
            --notes "Commit: ${{ github.sha }}" \
            app/build/outputs/apk/**/*.apk \
            version.json